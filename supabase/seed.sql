-- ============================================================
-- AssetVault — Seed Data
-- ============================================================
-- Default tags are already seeded in the migration.
-- This file can be used for local development test data.

-- Example: add extra default tags if needed
-- INSERT INTO public.tags (user_id, name, is_default) VALUES
--   (NULL, 'VFX', TRUE),
--   (NULL, 'Animation', TRUE);
